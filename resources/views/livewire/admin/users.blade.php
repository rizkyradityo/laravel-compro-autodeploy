1.  **Analyze the Request:**
    *   Input: `ORIGINAL CODE` (a Blade template snippet for user management) and `SUGGESTED EDIT` (a change to how admin status is checked).
    *   Task: Apply the edit to the original code and output the complete modified file.
    *   Constraints:
        *   Output ONLY code.
        *   Do NOT explain, summarize, or describe changes.
        *   Leave existing comments (though there aren't really any in the snippet, I should keep the structure intact).
        *   Preserve all unchanged code exactly as-is.

2.  **Analyze the Edit:**
    *   Original snippet:
